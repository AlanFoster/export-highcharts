Simple wrapper around the highcharts-export-server docs.


Update `input.js` with your graph configuration.
If you have additional needs, you can implement `callback.js`

Running -

```shell
  npm install
  npm run create
```